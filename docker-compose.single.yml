version: '3.8'

services:
  biodockify:
    build:
      context: .
      dockerfile: Dockerfile.single
    ports:
      - "8000:8000"
    volumes:
      - biodockify-data:/app/data

volumes:
  biodockify-data:
